name: sos_battery
description: SOS-BATTERY - Real-time battery rescue across USA
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_messaging: ^16.0.4
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  google_maps_flutter: ^2.14.0 # downgrade 2.24 tu 2.19.
  geolocator: ^14.0.2
  background_locator_2: ^2.0.0   # ← THAY BẰNG CÁI NÀY, FIX LỖI ANDROID 100%
  confetti: ^0.8.0
  lottie: ^3.1.2
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

#flutter:
  #uses-material-design: true
 # assets:
   # - assets/animations/