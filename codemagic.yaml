workflows:
  ios-build:
    name: SOS-BATTERY iOS Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    environment:
      flutter: 3.38.1
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
    scripts:
      - name: Clean & Setup
        script: |
          echo "=== CLEAN & SETUP iOS ==="
          flutter clean
          flutter pub get
          cd ios && pod install --repo-update && cd ..
      - name: Build IPA
        script: |
          echo "=== BUILDING IPA ==="
          flutter build ipa --release --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa                                 # ← FILE IPA CHÍNH THỨC (100% CÓ)
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/Products/Release-iphoneos/*.app.dSYM
      - flutter_drive.log

  android-build:
    name: SOS-BATTERY Android Build
    instance_type: mac_mini_m2
    environment:
      flutter: 3.38.1
    triggering:
      events:
        - push
    scripts:
      - name: Clean & Build AAB
        script: |
          echo "=== CLEAN & BUILD AAB ==="
          flutter clean
          flutter pub get
          flutter build appbundle --release
    artifacts:
      - build/app/outputs/bundle/release/app-release.aab    # ← FILE AAB CHÍNH THỨC
      - build/app/outputs/flutter-apk/app-release.apk       # ← BONUS: có luôn APK
      - flutter_drive.log
